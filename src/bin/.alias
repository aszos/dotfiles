#!/bin/zsh

# cd
alias cdp="cd ${PROJECTS}
alias cddot="cd ${DOTFILES_PROJECT}

# grep
alias grep="grep --color"
alias egrep="egrep --color=auto"
alias fgrep="fgrep --color=auto"

# ls
alias ls="ls -AhG"

# dotfiles
alias resource="source ${HOME}/.zshrc" 
